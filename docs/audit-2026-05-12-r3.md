# Code Audit Report — Third Pass

**Date:** 2026-05-12
**Scope:** 第二轮修复验证 + 残留问题

---

## R2 审计问题修复状态

| ID | 问题 | 提交 | 状态 |
|----|------|------|------|
| N1 | sklearn 未在 requirements.txt | `625f946` | **已修复** — 加入 `scikit-learn>=1.3.0` |
| N2 | 模块级 numpy/sklearn 导入 | `625f946` | **已修复** — 改为 calibrate() 内部 lazy import，ImportError 时优雅降级 |
| N3 | useSSE.ts 过时注释 | `0e26ae5` | **已修复** — 删除矛盾注释 |
| N4 | P2.3 文件未提交 | `107b3f4` | **已修复** — 5 个文件已提交 |
| N5 | tsbuildinfo 构建产物 | `2251ad2` | **部分修复** — 见下方残留问题 |

---

## 残留问题

### R1. tsbuildinfo 仍在 git 跟踪中（LOW）

**文件：** `frontend/tsconfig.tsbuildinfo`

`.gitignore` 已添加 `frontend/tsconfig.tsbuildinfo`（commit `2251ad2`），但该文件之前已被 git 跟踪。`.gitignore` 只对未跟踪文件生效，已跟踪文件需要用 `git rm --cached` 移除跟踪。

**现象：** `git status` 仍显示该文件为 modified。

**修复：**
```bash
git rm --cached frontend/tsconfig.tsbuildinfo
git commit -m "chore: stop tracking tsconfig.tsbuildinfo"
```

---

## 整体健康检查

### 启动链路验证

```
main.py imports:
  app.api.routes.backtest      → backtest.py 存在 ✓
  app.api.routes.calibration   → calibration.py 存在 ✓
  app.api.routes.sse           → sse.py 存在 ✓
  app.api.routes.notifications → notifications.py 存在 ✓

lifespan imports:
  news_fetcher_service  → 存在 ✓
  outcome_tracker       → 存在 ✓

calibration_service imports:
  sklearn (lazy)  → ImportError 时优雅降级 ✓
  numpy (lazy)    → ImportError 时优雅降级 ✓
```

### 依赖完整性

| 依赖 | requirements.txt | 说明 |
|------|:---:|------|
| fastapi | ✓ | |
| sqlalchemy | ✓ | |
| aiosqlite | ✓ | |
| numpy | ✓ | |
| scikit-learn | ✓ | R2/N1 修复 |
| httpx | ✓ | GDELT/ACLED 客户端需要 |

### 未跟踪文件

仅剩 `.superpowers/` 目录（已在 gitignore），无其他残留。

---

## 总结

| 级别 | 数量 | 说明 |
|------|------|------|
| CRITICAL | 0 | — |
| HIGH | 0 | — |
| MEDIUM | 0 | — |
| LOW | 1 | R1: tsbuildinfo 需 `git rm --cached` |

**从首次审计的 12 个问题，到第二次的 5 个，到现在的 1 个残留。** 代码库已达到可部署状态。

建议执行 `git rm --cached frontend/tsconfig.tsbuildinfo` 后即可推送。
